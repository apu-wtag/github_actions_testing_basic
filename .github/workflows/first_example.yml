name: my first workflow
on: push

jobs:
  first_job: 
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: Welcome message
        run: |
         echo "Hello world"
         echo "listing files"
         ls -la
         echo "cating readme"
         cat README.md
